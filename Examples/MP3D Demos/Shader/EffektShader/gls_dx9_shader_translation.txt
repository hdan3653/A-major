This is a little translation map,
will be actuliced 

MP

GSL Shader		directx Shader

mat2 rotation;          float2x2 rotation2;
                        matrix<float, 2 ,2> rotation2;

rotation * center;      mul(rotation, center)

vec2 coordRot;          float2 coordRot2;

vec3 Tex;               float3 Tex;

vec4 col;               float 4 col; 

mod(time, 6.0);         fmod(time, 6.0);

ivec2 tt;               int2 tt;

texture2D(tex0, coord)  tex2D(tex0, coord)

mix (genType x, genType y, genType a) ;lerp(a, b, s)